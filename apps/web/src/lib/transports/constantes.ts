// Rayon de recherche partagé par les sources de transport en commun (PRIM) et de Vélib' — une
// seule valeur, pour que tout texte qui la mentionne (dont les points d'attention) reste cohérent
// si elle évolue un jour.
export const RAYON_METRES_TRANSPORTS = 500;
