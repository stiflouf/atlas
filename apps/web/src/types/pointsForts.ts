export type PointFort = {
  id: string; // identifiant stable de la règle qui l'a produit
  texte: string; // texte factuel, jamais reformulé après coup
  provenance: string; // origine du raisonnement, affichée telle quelle
};
